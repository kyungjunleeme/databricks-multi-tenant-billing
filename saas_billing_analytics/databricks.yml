# This is a Databricks asset bundle definition for saas_billing_analytics.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: saas_billing_analytics
  uuid: 3018798a-90e6-47ea-9107-930f911f420d

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
#  dev:
#    # The default target uses 'mode: development' to create a development copy.
#    # - Deployed resources get prefixed with '[dev my_user_name]'
#    # - Any job schedules and triggers are paused by default.
#    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
#    mode: development
#    default: true
#    workspace:
#      host: https://dbc-ac824534-b453.cloud.databricks.com
#    variables:
#      catalog: workspace
#      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-ac824534-b453.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/kyungjunlee.me@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/kyungjunlee.me@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: saas_billing_analytics
      schema: prod
    permissions:
      - user_name: kyungjunlee.me@gmail.com
        level: CAN_MANAGE
